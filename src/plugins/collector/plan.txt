2 custom shapes (collector and collector filter)
output shape defines custom UI like folder but instead shows a dropdown where you can select what gets output here (maybe use attachments schema where filetype gets serialized)(also add regex somehow so that files can be filtered by name)
outputs check on collision if the partner is an input and attach on collision by using tldraw's group
inputs register outputs on collision (or on group would be smoother) and store them in a reference
when a file collides with the input the outputs are iterated and the condition of each output is evaluated against the colliding shape
when an output returns true it will use the editor to put the item at the location of the output

collector's have custom UI that decides what they collect, collectors can be put together and act as a sorting system
when an item intersects with a collector all connected collectors evaluate the item and if the coniditon is true then the item get teleported to that collector
(could attract all items within a certain range if the valuation fits)